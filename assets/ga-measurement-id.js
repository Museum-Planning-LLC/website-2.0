/**
 * Google Tag Manager — container ID from Tag Manager → Admin → Install Google Tag Manager.
 * Configure GA4 and other tags in GTM so hits are not duplicated on the page.
 */
window.GTM_CONTAINER_ID = "GTM-PGG4KV35";

/**
 * Optional legacy path: direct GA4 via gtag only when GTM is not in use.
 * Leave empty when GA4 is published exclusively through GTM.
 */
window.GA4_MEASUREMENT_ID = "";
